version: 2.1

orbs:
  python: circleci/python@1.2

workflows:
  sample:
    jobs:
      - build-and-test


jobs:
  build-and-test:
    working_directory: C:\Users\Vikas\AppData\Local\Programs\Python\Python39
    docker:
      - image: circleci/python:3.9.5
    steps:
      - checkout
      - run:
          name: Run tests
          command: |
            sudo pip install pipenv
            pipenv install
            
